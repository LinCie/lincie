/**
 * The single page-container measure: `content-max` with the documented mobile and
 * desktop gutters. Shared by every page and layout shell so the measure cannot drift.
 */
export const siteContainer =
  "mx-auto w-[calc(100%-40px)] max-w-[1152px] max-[480px]:w-[calc(100%-32px)] md:w-[calc(100%-80px)]";
